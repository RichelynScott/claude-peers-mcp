/**
 * claude-peers
 *
 * Peer discovery and messaging for Claude Code instances.
 *
 * This package has two entry points:
 *   - src/server.ts  — MCP server (spawned by Claude Code, one per instance)
 *   - src/broker.ts  — Shared broker daemon (auto-launched, one per machine)
 *
 * See README.md for setup and usage.
 */

export { } // module marker
